import { styled } from "styled-components";
import tw from "twin.macro";

export const StyledMultiSelect = styled.div(() => [tw`flex flex-col gap-2`]);

export const StyledDropdownButtonWrapper = styled.div(() => [tw`relative`]);
