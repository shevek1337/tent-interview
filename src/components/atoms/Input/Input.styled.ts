import { styled } from "styled-components";
import tw from "twin.macro";

export const StyledInput = styled.input(() => [
  tw`p-3 pr-2 text-sm h-10 border rounded-md bg-primary-white text-text-black-900 border-border-gray-400 w-full`,
]);
